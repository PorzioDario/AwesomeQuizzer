import React from 'react';
//import classes from './QuizProperties.module.css';

const QuizProperties = (props) => {
    return (
        <React.Fragment>
            <p>Properties of the quiz</p>
        </React.Fragment>
    );
}

export default QuizProperties;